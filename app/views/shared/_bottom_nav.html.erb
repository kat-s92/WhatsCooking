<%=link_to "Home", root_path %>
<%=link_to "Search", root_path %>
<%=link_to "Saved Recipes", saved_recipes_path %>
<% if @missing_item.present?
 ShoppingCart.find_by(user_id: current_user.id) %>
<%=link_to "Shopping Cart", shopping_cart_path(ShoppingCart.find_by(user_id: current_user.id).id) %>
<% else %>
  <%= nil %>
<% end %>
